/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { RecipeService } from "../recipe.service";
import { RecipeCreateInput } from "./RecipeCreateInput";
import { Recipe } from "./Recipe";
import { RecipeFindManyArgs } from "./RecipeFindManyArgs";
import { RecipeWhereUniqueInput } from "./RecipeWhereUniqueInput";
import { RecipeUpdateInput } from "./RecipeUpdateInput";
import { IngredientFindManyArgs } from "../../ingredient/base/IngredientFindManyArgs";
import { Ingredient } from "../../ingredient/base/Ingredient";
import { IngredientWhereUniqueInput } from "../../ingredient/base/IngredientWhereUniqueInput";

export class RecipeControllerBase {
  constructor(protected readonly service: RecipeService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Recipe })
  async createRecipe(@common.Body() data: RecipeCreateInput): Promise<Recipe> {
    return await this.service.createRecipe({
      data: data,
      select: {
        createdAt: true,
        id: true,
        recipeIngredients: true,
        recipeTitle: true,
        recipeUrl: true,
        title: true,
        updatedAt: true,
        url: true,
        userRelation: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [Recipe] })
  @ApiNestedQuery(RecipeFindManyArgs)
  async recipes(@common.Req() request: Request): Promise<Recipe[]> {
    const args = plainToClass(RecipeFindManyArgs, request.query);
    return this.service.recipes({
      ...args,
      select: {
        createdAt: true,
        id: true,
        recipeIngredients: true,
        recipeTitle: true,
        recipeUrl: true,
        title: true,
        updatedAt: true,
        url: true,
        userRelation: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Recipe })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async recipe(
    @common.Param() params: RecipeWhereUniqueInput
  ): Promise<Recipe | null> {
    const result = await this.service.recipe({
      where: params,
      select: {
        createdAt: true,
        id: true,
        recipeIngredients: true,
        recipeTitle: true,
        recipeUrl: true,
        title: true,
        updatedAt: true,
        url: true,
        userRelation: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Recipe })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async updateRecipe(
    @common.Param() params: RecipeWhereUniqueInput,
    @common.Body() data: RecipeUpdateInput
  ): Promise<Recipe | null> {
    try {
      return await this.service.updateRecipe({
        where: params,
        data: data,
        select: {
          createdAt: true,
          id: true,
          recipeIngredients: true,
          recipeTitle: true,
          recipeUrl: true,
          title: true,
          updatedAt: true,
          url: true,
          userRelation: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Recipe })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async deleteRecipe(
    @common.Param() params: RecipeWhereUniqueInput
  ): Promise<Recipe | null> {
    try {
      return await this.service.deleteRecipe({
        where: params,
        select: {
          createdAt: true,
          id: true,
          recipeIngredients: true,
          recipeTitle: true,
          recipeUrl: true,
          title: true,
          updatedAt: true,
          url: true,
          userRelation: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Get("/:id/ingredients")
  @ApiNestedQuery(IngredientFindManyArgs)
  async findIngredients(
    @common.Req() request: Request,
    @common.Param() params: RecipeWhereUniqueInput
  ): Promise<Ingredient[]> {
    const query = plainToClass(IngredientFindManyArgs, request.query);
    const results = await this.service.findIngredients(params.id, {
      ...query,
      select: {
        createdAt: true,
        id: true,
        name: true,
        quantity: true,

        recipe: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/ingredients")
  async connectIngredients(
    @common.Param() params: RecipeWhereUniqueInput,
    @common.Body() body: IngredientWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      ingredients: {
        connect: body,
      },
    };
    await this.service.updateRecipe({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/ingredients")
  async updateIngredients(
    @common.Param() params: RecipeWhereUniqueInput,
    @common.Body() body: IngredientWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      ingredients: {
        set: body,
      },
    };
    await this.service.updateRecipe({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/ingredients")
  async disconnectIngredients(
    @common.Param() params: RecipeWhereUniqueInput,
    @common.Body() body: IngredientWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      ingredients: {
        disconnect: body,
      },
    };
    await this.service.updateRecipe({
      where: params,
      data,
      select: { id: true },
    });
  }
}
